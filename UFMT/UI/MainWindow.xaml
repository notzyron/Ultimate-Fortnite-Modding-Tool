<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="UFMT.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UFMT"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="UFMT">

    <Window.SystemBackdrop>
        <DesktopAcrylicBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="#bb000000">
        <NavigationView x:Name="NavView" 
                        OpenPaneLength="200" 
                        ItemInvoked="NavView_ItemInvoked"
                        Loaded="NavView_Loaded">
            <NavigationView.MenuItems>
                <NavigationViewItem Tag="SkinsPage" Height="100" SelectsOnInvoked="True">
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Image Source="ms-appx:///Assets/SkinsIcon.png" Width="28" Height="28"/>
                        <TextBlock Text="Skins" FontSize="20" VerticalAlignment="Center"/>
                    </StackPanel>
                </NavigationViewItem>
                <NavigationViewItem Tag="EmotesPage" Height="100" SelectsOnInvoked="True">
                    <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                        <Image Source="ms-appx:///Assets/EmotesIcon.png" Width="32" Height="32"/>
                        <TextBlock Text="Emotes" FontSize="20" VerticalAlignment="Center"/>
                    </StackPanel>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <Frame x:Name="ContentFrame"/>
            </NavigationView.Content>
        </NavigationView>

    </Grid>
</Window>